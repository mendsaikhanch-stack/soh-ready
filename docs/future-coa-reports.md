# 🔮 Хойшлуулсан: CoA санхүү/татвар/ил тод тайлан модуль

**Шийдвэр (2026-06-23):** Play Store **launch-ийн ДАРАА** хийнэ. Одоо энергийг
store-д гаргахад зарцуулна. Энэ файл нь дизайн + хийх ёстой засваруудыг хадгална.

---

## ✅ 1-р үе хийгдэв — 2026-09-02

**B зам сонгосон** (байгаа датаг адаптчлах). Хийсэн зүйл:

- `payables` хүснэгт + `/admin/finance` ▸ **Өглөг** таб (`supabase-payables-migration.sql`)
- `/admin/finance` ▸ **Жилийн нэгтгэл** таб — сараар орлого/зардал/зөрүү, ангиллын задаргаа
- `/admin/finance/report` — **хэвлэх/PDF жилийн тайлан**, гарын үсгийн мөртэй
- Excel экспорт 5 хуудастай болов (орлого, зардал, сараар, авлага, өглөг)

Үүгээр `docs/SPEC.md` #6-ийн 4 заавал хэсэг бүрдсэн.

**Гол саад одоо ч дата хэвээр:** 2026-09-02-ны байдлаар 8 идэвхтэй СӨХ-өөс
зөвхөн демо дээр зардал бүртгэгдсэн (`budget_items` 28 мөр, бүгд #2678),
`payments` 8 мөр, `invoices` 4 мөр. Тайлангийн модуль бэлэн ч **дата ороогүй
бол хоосон гарна**. Дараагийн алхам нь тайлан биш — И-Биллингээр орлогыг
автоматаар оруулах (`docs/ebilling-ebarimt.md` §2.1) ба даргад зардлаа
бүртгүүлэх зуршил суулгах.

## ✅ 2-р үе хийгдэв — 2026-09-02 (мөн өдөр)

Дээрх «3 тайлан» зорилтоос:

| Зорилт | Төлөв |
|---|---|
| 1. Санхүүгийн тайлан (баланс + орлого-үр дүн) | ✅ `/admin/finance/statements` |
| 2. Татварын тайлан | 🟡 **тооцоо** гарна (ААНОАТ 10%), ТТ-02 маягт БИШ |
| 3. «Шилэн СӨХ» тайлан | 🟡 оршин суугч зардлаа сар/жилээр харна; орлого, цуглуулалтын хувь ХАРАГДАХГҮЙ |

Нэмж хийсэн:

- **Хамгийн чухал засвар:** «Зардал нэмэх» товч хэзээ ч ажиллаагүй байсан
  (`budget_items.name` NOT NULL, апп нь илгээдэггүй, алдааг залгидаг байв).
  7 бодит СӨХ-ийн нэг нь ч зардлаа оруулж чадаагүйн ГОЛ шалтгаан энэ.
- **Бусад орлого** — `budget_items.type='income'`. Татварын хувьд гишүүдийн
  хураамжаас тусад нь тооцох ёстой тул салгав.
- **Мөнгөн гүйлгээний тайлан** — эхний үлдэгдлээс эхлэн сараар.

### Дараагийн зам (3-р үе)

- Давхар бичилтийн `ledger_lines` + дансны төлөвлөгөө — **шаардлага гарвал**.
  8 идэвхтэй СӨХ-д хэт том, одоогийн B зам хангалттай.
- ТТ-02(2) маягтын бодит бүтцээр экспорт — маягтын загварыг нягтлангаас
  авах шаардлагатай. Загваргүйгээр таамгаар бичих нь эрсдэлтэй.
- Оршин суугчид ОРЛОГО, цуглуулалтын хувийг харуулах — `payments` нь RLS-ээр
  хаалттай тул нэгтгэсэн тоо буцаадаг тусдаа API хэрэгтэй.
- Цалингийн ХХОАТ, НДШ тооцоо — одоо `staff` хүснэгттэй холбогдоогүй.

### Баталгаажуулах шаардлагатай (нягтлан / татварын байцаагч)

1. Гишүүдийн сарын хураамж ААНОАТ-д ногдох уу? (одоо ногдуулаагүй гэж үзсэн)
2. Бусад орлогод 10% зөв үү, хөнгөлөлт бий юу?
3. СӨХ жилийн санхүүгийн тайланг ямар маягтаар тушаадаг вэ?

---

## Зорилго (3 тайлан нэг endpoint-оос)
1. **Санхүүгийн тайлан** — Баланс (актив/эх үүсвэр) + Орлого-үр дүн
2. **Татварын тайлан** — ТАХ-ын TT-02(2) формат руу импортлох
3. **"Шилэн СӨХ" тайлан** — оршин суугчдад график, цуглуулалтын хувь

## ⚠️ As-is ажиллахгүй — засах ёстой зүйлс
Анх ирүүлсэн `app/api/coa-reports/route.ts` нь одоогийн схемтэй таарахгүй:

1. **`ledger_lines` хүснэгт байхгүй** — давхар бичилтийн ledger (debit/credit,
   account_code, coa_id) апп-д огт алга. Бүх тайлан үүн дээр тулгуурласан.
2. **`invoices.coa_id` байхгүй** — invoices нь `sokh_id`-тэй. `coa_id` → `sokh_id`.
3. **Status том үсэг** — код `'PENDING'`/`'PAID'`, схем жижиг `'pending'`/`'paid'`
   → авлага/цуглуулалт үргэлж 0 гарна. Заавал засна.
4. **Буруу env** — `process.env.SUPABASE_URL` → `NEXT_PUBLIC_SUPABASE_URL`.
5. **🔒 Auth огт байхгүй** — service-role key, нэвтрэлт шалгахгүй endpoint нь
   дурын СӨХ-ийн санхүү/татварыг ил болгоно. Заавал auth + tenant-scope нэмэх.
6. **Module-scope `createClient(undefined)`** — #4-тэй хослоод build унагаж магадгүй.
   Env-ийг handler дотор уншина.

## Хоёр зам
- **A. Жинхэнэ давхар бичилт** — `ledger_lines` + дансны төлөвлөгөө, гүйлгээ бүрт
  (нэхэмжлэх төлөгдөх, зардал) ledger бичих. Audit/татварт бэлэн ч том ажил.
- **B. Одоогийн датаг адаптчлах** — орлого `invoices`/`payments`-аас, зардал
  `budget_items`-аас, авлага төлөгдөөгүй нэхэмжлэхээс. Давхар бичилтгүй, хурдан,
  одоо ажиллана. **Ихэнх СӨХ-д хангалттай** (audit-grade татвар хэрэгтэй болтол).

## Дансны кодын зураглал (анхны санаа)
| Код | Утга |
|---|---|
| 1010 | Касс/банк (актив, D−C) |
| 1210 | Авлага (төлөгдөөгүй нэхэмжлэх) |
| 3110 | Хуримтлагдсан ашиг |
| 4010 | Үндсэн орлого — гишүүдийн хураамж (татваргүй) |
| 4020 | Бусад орлого — түрээс/алданги (10% татвартай) |
| 5010 | Цалин, НДШ |
| 5020 | Тог/цахилгаан/ус |
| 5030 | Лифт/хог/засвар |

> Татвар: СӨХ зөвхөн **бусад орлого (4020)** дээр 10% төлнө гэсэн таамаг —
> launch дараа татварын мэргэжилтнээр баталгаажуулах.

## Анх ирүүлсэн код (лавлахад)
~~~ts
// app/api/coa-reports/route.ts  (DRAFT — as-is ажиллахгүй, дээрх засваруудыг хий)
import { NextResponse } from 'next/server';
import { createClient } from '@supabase/supabase-js';

const supabase = createClient(process.env.SUPABASE_URL!, process.env.SUPABASE_SERVICE_ROLE_KEY!);

export async function GET(request: Request) {
  // ... coa_id, start_date, end_date параметр
  // ledger_lines-аас account_code (1010/4010/4020/5010/5020/5030)-оор debit/credit нэгтгэх
  // invoices-аас PENDING авлага, PAID цуглуулалтын хувь
  // → financialReport (balance_sheet + income_statement)
  // → taxReport (TT-02(2), 4020 × 10%)
  // → residentsReport (income_chart, expense_chart, collection_rate)
}
~~~
Бүрэн эх кодыг чат түүх / энэ commit-ийн өмнөх ярианаас үзнэ үү.

## Launch дараа эхлэх алхам
1. A/B замаа сонгох (ихэнхдээ **B**-ээр эхэл).
2. #3, #4, #5 (status, env, auth+tenant-scope) заавал засах.
3. `coa_id` → `sokh_id` болгох.
4. UI: оршин суугчид "Шилэн СӨХ" график, админд татварын экспорт.
